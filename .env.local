# Arquivo para desenvolvimento LOCAL — aponta para o servidor rodando na sua máquina.
# Este arquivo NÃO deve ser commitado (já está no .gitignore do CRA por padrão).
# Para usar o Railway em dev, remova ou renomeie este arquivo.
REACT_APP_API_URL=http://localhost:3000
PORT=3001
